# 「研究现状」PPT 页面内容指南

> 适用于："海蓝杯"第五届声学应用研究与设计大赛·创新赛道  
> 项目名称：基于 AUV 的路径规划及其界面设计  
> 章节：第一章 项目概述 → 研究现状

---

## 一、故事/案例导入（配图上方的引入语）

建议在表格上方用 1-2 句话引入一个真实场景，例如：

> 随着海洋资源开发与水下探测需求的持续增长，AUV 已广泛应用于深海测绘、管道巡检、海底矿藏勘探等领域。然而，现有主流路径规划算法在面对复杂地形与动态障碍物时，往往暴露出覆盖效率低、路径不连续、工程落地难等共性瓶颈。

---

## 二、对比表格内容（每列一种算法）

### 表格设计思路

- **行**：评价维度（市场/工程关注的痛点）  
- **列**：现有代表性算法（不涉及本项目方案，仅作引入）  
- **符号建议**：✅ 较好 / ⚠️ 一般 / ❌ 较差（或用 ★ 评分）

### 推荐列（五种典型算法）

| 评价维度 | 传统梳状路径法 | 人工势场法 | A\* 算法 | RRT 算法 | 遗传算法 |
|---|---|---|---|---|---|
| **覆盖完整性** | ✅ 高（全区域覆盖） | ❌ 低（局部收敛） | ⚠️ 中（依赖网格划分） | ❌ 低（随机探索） | ⚠️ 中（依赖种群设计） |
| **动态避障能力** | ❌ 无法避障 | ⚠️ 仅局部避障 | ⚠️ 需重规划 | ✅ 较好 | ❌ 差（离线计算） |
| **路径连续性/光滑性** | ⚠️ 折线转弯，不平滑 | ✅ 路径连续 | ❌ 折线路径 | ❌ 非光滑 | ❌ 离散点，需后处理 |
| **计算实时性** | ✅ 高（简单几何） | ✅ 高 | ⚠️ 中（大图搜索慢） | ⚠️ 中 | ❌ 低（迭代慢） |
| **复杂地形适应性** | ❌ 忽略地形起伏 | ⚠️ 易陷入局部最优 | ⚠️ 中（需精细建图） | ✅ 较好 | ✅ 较好（全局搜索） |
| **工程落地难度** | ✅ 低（实现简单） | ⚠️ 中 | ⚠️ 中 | ⚠️ 中 | ❌ 高（参数调优复杂） |
| **多 AUV 协同支持** | ❌ 不支持 | ❌ 不支持 | ❌ 不支持 | ❌ 不支持 | ⚠️ 理论可扩展 |

> **说明**：可根据PPT风格将 ✅/⚠️/❌ 替换为彩色图标（绿/黄/红圆点），视觉效果更佳。

### 各算法简介（用于表格下方注释或旁注气泡）

1. **传统梳状路径法（Boustrophedon / Lawnmower）**  
   最经典的全覆盖算法，路径呈"S"形往复扫描，实现简单但转弯处路径不连续，无法应对障碍物，海底地形起伏时覆盖率下降明显。

2. **人工势场法（APF, Artificial Potential Field）**  
   将目标设为引力、障碍物设为斥力，AUV 在合力场中运动。实时性好，但易陷入局部极小值（如狭窄通道），且无法保证全区域覆盖。

3. **A\* 算法**  
   经典启发式图搜索算法，路径最优性有理论保证，但在大规模海底地图中计算量大、路径为折线不够平滑，难以直接驱动 AUV 执行。

4. **RRT 算法（快速随机扩展树）**  
   通过随机采样构建路径树，适合高维空间规划，避障能力强，但路径随机性高、不光滑，且不保证全覆盖。

5. **遗传算法（GA, Genetic Algorithm）**  
   全局优化能力强，可同时优化路径长度和覆盖率，但参数敏感、计算耗时，不适合在线实时规划场景。

---

## 三、表格下方痛点总结语（建议用加粗或高亮框展示）

### 推荐写法（对应表格缺陷，自然引出需求）

> **现有算法均在「路径光滑性」「全覆盖保障」「复杂地形适应」「工程集成」四个维度中存在明显短板**，难以同时满足 AUV 实际作业的多重需求——这正是本项目研究的核心驱动力。

或更简洁的版本：

> 单一算法难以兼顾路径覆盖率、平滑性与实时避障，现有工具缺乏面向 AUV 全任务链的集成规划平台。

---

## 四、配图搜索建议

### 1. 关键词推荐（中英文均可）

| 图片类型 | 推荐搜索关键词（英文） | 推荐搜索关键词（中文） |
|---|---|---|
| 梳状路径示意图 | `lawnmower coverage path AUV` | `梳状路径规划 AUV 全覆盖` |
| 人工势场避障 | `artificial potential field path planning obstacle` | `人工势场法路径规划` |
| A* 路径搜索 | `A star algorithm path planning visualization` | `A*算法路径规划可视化` |
| RRT 树形扩展 | `RRT rapidly exploring random tree path` | `RRT路径规划图` |
| 遗传算法收敛 | `genetic algorithm optimization path` | `遗传算法路径优化` |
| AUV 水下作业 | `AUV autonomous underwater vehicle seabed survey` | `AUV海底探测 水下机器人` |

### 2. 推荐图片来源（高质量、可用于学术PPT）

| 来源 | 特点 | 网址 |
|---|---|---|
| **Google 学术图片** | 论文配图，学术感强，质量高 | 搜索论文后查看 Figures |
| **ResearchGate** | 免费论文图表，直接可用 | researchgate.net |
| **IEEE Xplore** | 权威期刊论文插图 | ieeexplore.ieee.org |
| **知网/万方** | 国内期刊插图，中文标注 | cnki.net |
| **Flaticon / Freepik** | 矢量图标，PPT美化用 | flaticon.com / freepik.com |
| **Unsplash** | 高清海洋/水下摄影图 | unsplash.com |

### 3. 论文数据库搜索技巧

在 **Google Scholar** 或 **IEEE Xplore** 搜索以下关键词，找到高引论文后直接截取其 Figure（注明来源）：
- `"coverage path planning" AUV underwater`
- `"path planning algorithm comparison" underwater vehicle`
- `"Dubins path" AUV obstacle avoidance`

### 4. 图片排布建议

- 在表格**上方**放 3-5 张算法效果对比图（横排），每张图下方标注算法名称
- 图片风格尽量统一：优先选择**白底或蓝色系**的矢量/仿真图
- 如找不到合适图片，可在 MATLAB 中运行本项目并截图生成演示图

---

## 五、完整页面布局建议（参考"图2"风格）

```
┌─────────────────────────────────────────────────────────┐
│  [引入故事句] 随着海洋探测需求持续增长……              │
├──────────┬──────────┬──────────┬──────────┬────────────┤
│  配图1   │  配图2   │  配图3   │  配图4   │  配图5    │
│ 梳状路径 │ 势场法   │  A*      │  RRT     │ 遗传算法  │
├──────────┴──────────┴──────────┴──────────┴────────────┤
│                    【对比表格】                         │
│  评价维度 │ 梳状路径 │ 势场法 │ A* │ RRT │ 遗传算法   │
│  ……       │  ✅/❌   │  …     │ … │  …  │    …       │
├─────────────────────────────────────────────────────────┤
│ ⚡ 痛点总结：现有算法难以同时满足……，亟需集成平台    │
└─────────────────────────────────────────────────────────┘
```

---

> 本文档由项目组整理，供"海蓝杯"参赛PPT制作参考使用。
